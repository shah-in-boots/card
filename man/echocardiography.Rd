% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/echo.R
\name{echocardiography}
\alias{echocardiography}
\alias{extract_la_size}
\alias{extract_lvef}
\alias{extract_lvidd}
\alias{extract_la_diameter}
\alias{extract_echo_findings}
\title{Extract Echocardiogram Measurements}
\usage{
extract_la_size(text, range = c("upper", "lower"))

extract_lvef(text, min_val = 5, max_val = 90)

extract_lvidd(text, min_val = 1, max_val = 10)

extract_la_diameter(text, min_val = 1, max_val = 10)

extract_echo_findings(text)
}
\arguments{
\item{text}{Character vector of echo report text}

\item{range}{Which end of a graded range such as \code{"mildly to moderately dilated"} to report. Defaults to \code{"upper"}, since a report describing such a
range is asserting the higher grade is present, and the conservative reading
is the one that does not under-call disease.}

\item{min_val, max_val}{Plausible range for the measurement, outside of which a
match is discarded. The defaults are \code{1} to \code{10} cm for
\code{extract_la_diameter()} and \code{extract_lvidd()}, and \code{5} to \code{90} percent for
\code{extract_lvef()}.}
}
\value{
\itemize{
\item \code{extract_la_size()}: Character vector of LA size (\code{"normal"}, \code{"mild"}, etc.)
\item \code{extract_la_diameter()}: Numeric LA diameter in cm
\item \code{extract_lvef()}: Numeric LVEF percentage
\item \code{extract_lvidd()}: Numeric LVIDd in cm
\item \code{extract_echo_findings()}: Tibble of key structure/function findings, one row per report
}
}
\description{
A set of functions to extract common echocardiogram measurements
from free text reports. These functions use regular expressions to identify and
extract both qualitative descriptions and quantitative measurements.

The following measurements can be extracted:
\itemize{
\item Left atrial (LA) size (qualitative description)
\item Left atrial diameter (quantitative measurement in cm)
\item Left ventricular ejection fraction (LVEF, percentage)
\item Left ventricular internal diameter in diastole (LVIDd, in cm)
\item A compact set of clinically important findings from a full report
}
}
\details{
These functions use regular expressions to parse unstructured text from
echo reports. They handle common variations in terminology and units. Measurements
outside plausible ranges are returned as NA.

A measurement is only read from the same clause as the term that names it, so
a value belonging to a neighboring structure is not attributed to the wrong
one. Where several candidate values appear, the first one inside \code{min_val} to
\code{max_val} is taken, which skips stray digits such as the "2d" in "LVEF by 2D
Simpson is 55\%". Linear dimensions are returned in centimeters. A report that
writes the units has them taken at face value; where the units are omitted,
as structured measurement fields often do, they are inferred from \code{min_val}
and \code{max_val} by reading the value as centimeters when that is plausible and
as millimeters when only the millimeter reading is. The two readings cannot
both be plausible for a chamber dimension, so \code{"LVIDd: 52"} resolves to 5.2
cm. This inference only applies where the surrounding text names the
measurement; the fallback search used by \code{extract_la_diameter()} matches on a
nearby keyword alone, and there the units are what mark a number as a
measurement at all, so they remain required.

All functions are vectorized over \code{text} and return one element (or row) per
report. Qualitative grades are normalized to \code{"none"}, \code{"trace"}, \code{"trivial"},
\code{"mild"}, \code{"moderate"}, and \code{"severe"} so they can be used directly as an
ordered factor.
}
\examples{
report <- "The left atrium is mildly to moderately dilated. LVEF is 55\%."
extract_la_size(report) # Returns "moderate"
extract_la_size(report, range = "lower") # Returns "mild"
extract_lvef(report) # Returns 55

# Units are inferred where a report leaves them out
extract_lvidd("LVIDd: 52") # Returns 5.2, as no ventricle is 52 cm across
extract_lvidd("LVIDd: 5.2") # Returns 5.2

}
