% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{maude_ownership}
\alias{maude_ownership}
\title{Corporate Ownership of Medical Device Manufacturers}
\format{
A \code{tbl_df} with six columns:
\itemize{
\item \strong{entity}: Character. The company that was owned.
\item \strong{parent}: Character. The company that owned it.
\item \strong{from}: Date. When the ownership began, \code{NA} for unbounded, which is
used where the relationship predates the period MAUDE covers.
\item \strong{to}: Date. When it ended, \code{NA} for ongoing. A closed interval is a
divestiture.
\item \strong{note}: Character. The precision of the date, and the nature of the
transaction where it is not a plain acquisition.
\item \strong{source}: Character. A citation, \code{NA} where none was recorded.
}

Intervals do not overlap within an entity, which is what lets
\code{\link[=resolve_maude_owner]{resolve_maude_owner()}} take the first matching row.
}
\usage{
maude_ownership
}
\description{
A dated parent-pointer table recording which company owned which, and when.
Maintained in \code{data-raw/maude-entities/ownership.csv} and walked by
\code{\link[=resolve_maude_owner]{resolve_maude_owner()}}, which follows an entity up the chain until no row
matches the date it was given.
}
\section{Why it is dated, and separate}{
MAUDE reports span 1992 to the present, and the company that made a device is
frequently not the company that owns that business now. Holding ownership as
its own table, rather than as a parent baked into each pattern, means an
acquisition is \strong{one row} and an acquisition of a parent carries every
entity beneath it. \code{"Telectronics Pacing Systems"} reaches Abbott through
St. Jude Medical without anyone writing Abbott next to Telectronics.

It also makes a divestiture expressible, which a single-parent column cannot
do. An entity with no row matching the date owns itself, so Physio-Control
resolves to Medtronic in 2005, to itself in 2014, and to Stryker in 2020.
}

\section{What earns a row}{
Three rules keep this from becoming a corporate database:
\enumerate{
\item An entity earns a row only if a pattern index names it, or if it sits on
a chain between one and its parent.
\item A row requires a closing date that can be stated. A relationship that is
real but undated -- Stockert building generators for Biosense Webster --
is recorded in the pattern index instead.
\item The chain stops at the operating company the field would name. ZOLL has
been an Asahi Kasei subsidiary since 2012, and nobody calls it an Asahi
Kasei defibrillator.
}

Where a precise closing date could not be established the row uses the start
of the month or year and says so in \code{note}, and \code{source} is \code{NA}. That is a
stated approximation rather than a confident-looking wrong date, and
\code{coverage.md} lists every row lacking a source so the gap stays visible.
}

\examples{
# The chain from a 1990s pacemaker company to its owner today
maude_ownership[maude_ownership$entity == "Telectronics Pacing Systems", ]
resolve_maude_owner(
  "Telectronics Pacing Systems",
  as_of = as.Date(c("2000-01-01", "2020-01-01"))
)

# A divestiture, which a single-parent column cannot express
maude_ownership[maude_ownership$entity == "Physio-Control", ]

}
\seealso{
\code{\link[=resolve_maude_owner]{resolve_maude_owner()}}, \link{maude_manufacturer_index}
}
\keyword{datasets}
