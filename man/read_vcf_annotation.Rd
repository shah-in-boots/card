% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genetics.R
\name{read_vcf_annotation}
\alias{read_vcf_annotation}
\title{Parse VEP-Annotated VCF Files}
\usage{
read_vcf_annotation(
  vcf_path,
  info_id = "CSQ",
  selected_fields = c("Consequence", "IMPACT", "SYMBOL", "Gene", "Feature", "BIOTYPE",
    "HGVSp", "HGVSc", "EXON", "INTRON", "SIFT", "PolyPhen", "LoF", "LoF_filter",
    "LoF_flags", "LoF_info"),
  impact_filter = NULL,
  lof_confidence = NULL,
  polyphen_min = NULL,
  sift_max = NULL
)
}
\arguments{
\item{vcf_path}{Path to a VCF file annotated with Ensembl VEP.}

\item{info_id}{INFO field identifier that stores the VEP annotations.}

\item{selected_fields}{Character vector of CSQ/ANN columns to retain. Columns
absent from the file are silently dropped.}

\item{impact_filter}{Optional character vector of \code{IMPACT} labels to retain
(e.g., \code{c("HIGH", "MODERATE")}).}

\item{lof_confidence}{Optional character vector of \code{LoF} confidence codes to
retain (typically \code{"HC"} or \code{"LC"}).}

\item{polyphen_min}{Optional numeric cutoff; keeps rows with parsed PolyPhen
probability greater than or equal to this value when available.}

\item{sift_max}{Optional numeric cutoff; keeps rows with parsed SIFT
probability less than or equal to this value when available.}
}
\value{
A tibble with base VCF columns (\code{CHROM}, \code{POS}, \code{ID}, \code{REF}, \code{ALT},
\code{QUAL}, \code{FILTER} when present) plus the selected CSQ/ANN annotation
columns. One row is returned per alternate allele consequence.
}
\description{
Convert an annotated VCF file into a tidy tibble by expanding the CSQ/ANN
annotation field and selecting commonly used consequence columns (e.g.,
\code{SIFT}, \code{PolyPhen}, \code{LoF}). Optional filters allow you to keep only variants
matching specific impact or loss-of-function confidence levels.
}
\examples{
\dontrun{
parsed <- read_vcf_annotation(
  "path/to/annotated.vcf.gz",
  selected_fields = c("Consequence", "IMPACT", "SYMBOL", "SIFT", "PolyPhen")
)
}

}
