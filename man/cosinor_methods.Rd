% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosinor-methods.R
\name{cosinor_methods}
\alias{cosinor_methods}
\alias{coef.cosinor}
\alias{vcov.cosinor}
\alias{sigma.cosinor}
\alias{nobs.cosinor}
\alias{df.residual.cosinor}
\alias{logLik.cosinor}
\title{Extract Cosinor Model Components}
\usage{
\method{coef}{cosinor}(object, type = c("cosinor", "linear", "all"), ...)

\method{vcov}{cosinor}(object, type = c("linear", "cosinor"), ...)

\method{sigma}{cosinor}(object, ...)

\method{nobs}{cosinor}(object, ...)

\method{df.residual}{cosinor}(object, ...)

\method{logLik}{cosinor}(object, ...)
}
\arguments{
\item{object}{model of class \code{cosinor}}

\item{type}{parameterisation to return. \code{"cosinor"} gives the mesor,
amplitudes and acrophases; \code{"linear"} gives the regression coefficients.
\code{coef()} additionally accepts \code{"all"}, which returns every stored
coefficient in the object's own order.}

\item{...}{not currently used, but required for extensibility}
}
\value{
\code{coef()} a named numeric vector; \code{vcov()} a named
\verb{(2p+1) x (2p+1)} matrix; \code{sigma()} a single numeric; \code{nobs()} and
\code{df.residual()} a single integer; \code{logLik()} an object of class \code{logLik}.
}
\description{
Standard extractor methods for a fitted \link{cosinor} model. Every
statistic the package reports is built from \code{\link[=vcov.cosinor]{vcov.cosinor()}}, so these are
the functions to reach for when writing a new one.
}
\details{
The individual cosinor is ordinary least squares on the design
matrix \verb{[1, x_1, z_1, ..., x_p, z_p]}, so \code{coef()}, \code{vcov()}, \code{sigma()},
\code{df.residual()} and \code{logLik()} agree exactly with the equivalent \link[stats:lm]{stats::lm}
fit. That equivalence is asserted in the package's tests and is the
cheapest available check on the fitting code.
}
\section{Parameterisations}{
\code{type = "linear"} returns the regression coefficients
\eqn{M, \beta_j, \gamma_j} that least squares solves for; \code{type = "cosinor"} returns the mesor, amplitudes and acrophases
\eqn{M, A_j, \phi_j} that are interpreted. The second is a nonlinear
function of the first, and its covariance is obtained by the delta method.
}

\section{Population models}{
For a population-mean cosinor the random unit is the subject, so \code{vcov()}
returns the between-subject covariance of the per-subject coefficients
divided by the number of subjects, on \eqn{k - 1} degrees of freedom.
\code{sigma()} and \code{logLik()} are not defined - a population fit never maximised
a single likelihood, so returning an \code{AIC} for it would be a plausible
wrong number rather than an answer.
}

\references{
Cornelissen G. Cosinor-based rhythmometry. \emph{Theoretical Biology
and Medical Modelling} 2014;11:16. \doi{10.1186/1742-4682-11-16}
}
\seealso{
\code{\link[=cosinor]{cosinor()}}, \code{\link[=confint.cosinor]{confint.cosinor()}}, \code{\link[=anova.cosinor]{anova.cosinor()}}
}
