% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genetics.R
\name{read_vcf_header}
\alias{read_vcf_header}
\title{Summarize VEP-Annotated VCF Headers}
\usage{
read_vcf_header(vcf_path, info_id = "CSQ")
}
\arguments{
\item{vcf_path}{Path to a VCF file annotated with Ensembl VEP.}

\item{info_id}{INFO field identifier that stores the VEP annotations. The
default (\code{"CSQ"}) matches the format produced by the \code{vep} command.}
}
\value{
A list with elements:
\describe{
\item{info_fields}{A tibble with parsed INFO metadata (ID, Number, Type,
Description) for each INFO line in the header.}
\item{csq_fields}{Character vector describing the pipe-delimited CSQ/ANN
annotation order as defined by VEP (e.g., \code{Allele}, \code{Consequence},
\code{IMPACT}, \code{SYMBOL}).}
\item{lof_codes}{Named list describing loss-of-function confidence codes
(high/low confidence). The codes are derived from the VEP LOF plugin
definitions (typically \code{HC} and \code{LC}).}
}
}
\description{
Inspect the INFO header lines of a VCF produced by Ensembl VEP to understand
which annotations are available (e.g., CSQ/ANN content) and how they are
encoded. This helper makes it easier to decide which columns to keep when
parsing the data table.
}
\details{
The Ensembl VEP header provides a \code{Format: ...} statement inside the INFO
line for the CSQ/ANN field. This function extracts those field names so that
downstream parsing can map each pipe-separated element to a column. If the
LOF plugin is present, the resulting data typically contain \code{LoF},
\code{LoF_filter}, \code{LoF_flags}, and \code{LoF_info} columns where \code{LoF} values of \code{HC}
(high confidence) or \code{LC} (low confidence) indicate the plugin's assessment.
}
\examples{
\dontrun{
header_info <- read_vcf_header("path/to/annotated.vcf.gz")
header_info$csq_fields
}

}
