% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{maude_manufacturer_index}
\alias{maude_manufacturer_index}
\title{MAUDE Manufacturer Index for Cardiovascular Devices}
\format{
A \code{tbl_df} with five columns:
\itemize{
\item \strong{pattern}: Character. A case-insensitive regular expression matched
against the raw MAUDE manufacturer string.
\item \strong{entity}: Character. The canonical name of the entity that made the
device.
\item \strong{priority}: Integer. Lower patterns are tested first, defaulting to
\code{100}. Only a row that exists to pre-empt another carries a different
value: \code{CRYOCATH} is \code{10} because reports arrive as
\code{"MEDTRONIC CRYOCATH LP"} and the plain \code{MEDTRONIC} pattern would
otherwise claim them.
\item \strong{note}: Character. Why the row maps the way it does, where that is not
obvious. \code{NA} where it is.
\item \strong{source}: Character. A citation for the mapping, \code{NA} where none was
recorded.
}

The table carries a \code{coverage} attribute, described above.
}
\usage{
maude_manufacturer_index
}
\description{
A regex lookup mapping the manufacturing entities that appear in MAUDE's
\code{manufacturer_name} field onto a canonical entity name. Maintained in
\code{data-raw/maude-entities/manufacturer-patterns.csv} and applied by
\code{\link[=normalize_maude_manufacturer]{normalize_maude_manufacturer()}}, which takes the first pattern that matches.
}
\details{
The entity is the company that made the device, not the company that owns it
today. Plants and contract manufacturers resolve here -- \code{"MPRI"} is
Medtronic, \code{"VENUSA DE MEXICO S.A. DE C.V."} builds under contract for
Abbott -- because neither is an ownership relationship and neither has a date.
Corporate ownership is \link{maude_ownership}, and is applied separately by
\code{\link[=resolve_maude_owner]{resolve_maude_owner()}}, because it is the part that changes with time.

Coverage is measured against the openFDA \code{count} endpoint each time the
table is rebuilt and travels with the data rather than with this page, which
would go stale on the next edit:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{attr(maude_manufacturer_index, "coverage")
}\if{html}{\out{</div>}}

The full report, including patterns that matched nothing and the largest
strings that fell through, is written to
\code{data-raw/maude-entities/coverage.md}. An entity the index does not cover
returns \code{NA} rather than a guess.
}
\examples{
# What the index knows about the Medtronic entities
maude_manufacturer_index[
  grepl("MEDTRONIC|CRYOCATH", maude_manufacturer_index$pattern),
]

# Measured coverage, as of the last rebuild
attr(maude_manufacturer_index, "coverage")

}
\seealso{
\code{\link[=normalize_maude_manufacturer]{normalize_maude_manufacturer()}}, \link{maude_ownership}
}
\keyword{datasets}
