% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosinor-methods.R
\name{glance.cosinor}
\alias{glance.cosinor}
\title{Glance At A Cosinor Model}
\usage{
\method{glance}{cosinor}(x, ...)
}
\arguments{
\item{x}{a \code{cosinor} object created by \code{\link[=cosinor]{cosinor()}}}

\item{...}{For extensibility}
}
\value{
a \code{tibble} with one row
}
\description{
One row summarising a fitted \link{cosinor} model.
}
\details{
\code{logLik}, \code{AIC} and \code{BIC} are \code{NA} for a population-mean cosinor,
which pools per-subject fits rather than maximising a single likelihood.

\code{kappa} is the design condition number, reported here because a model whose
periods the data cannot separate is otherwise indistinguishable from a good
one in this table. See \link{cosinor_identifiability}.
}
\examples{
data(twins)
glance(cosinor(rDYX ~ hour, twins, tau = c(24, 12)))

}
