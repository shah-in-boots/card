% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vep.R
\name{read_vep_data}
\alias{read_vep_data}
\title{Read VEP Output File}
\usage{
read_vep_data(
  file,
  format = c("tab", "vcf"),
  columns = NULL,
  parse_extra = TRUE
)
}
\arguments{
\item{file}{Path to the VEP output file. Can be uncompressed or gzipped.}

\item{format}{Character string specifying the output format. One of:
\describe{
\item{"tab"}{Default VEP tab-delimited output format (default)}
\item{"vcf"}{VCF format output (produced with \code{--vcf} flag)}
}}

\item{columns}{Character vector of column names to include in the output.
Default is NULL (returns all columns). Can reference both fixed VEP
columns (e.g., "Gene", "Consequence") and Extra/CSQ fields (e.g.,
"SYMBOL", "IMPACT", "SIFT", "LoF").}

\item{parse_extra}{Logical. Whether to parse the Extra column (tab) or CSQ
field (VCF) into individual columns. Default is TRUE.}
}
\value{
A tibble with one row per variant-transcript annotation. If
parse_extra is TRUE, the Extra/CSQ column is replaced by its parsed
key-value pairs as individual columns. VEP dashes ("-") are replaced with
NA. The returned tibble has the following attributes:
\describe{
\item{vep_header}{The complete header structure from read_vep_header()}
\item{source_file}{Normalized path to the source VEP file}
}
}
\description{
Read a VEP output file into a tidy tibble.
}
\details{
\code{read_vep_data()} parses tab-delimited and VCF-style VEP outputs and returns one
row per annotated consequence record. By default it expands \code{Extra} (tab) or
\code{CSQ} (VCF) into separate columns for easier downstream filtering.

This function uses \code{\link{read_vep_header}} to infer structure and then
reads the data body in a single pass.

\strong{Tab-delimited format:}
\itemize{
\item 14 columns: 13 fixed fields plus "Extra" column
\item Extra contains semicolon-delimited key=value pairs
\item Standalone flags are converted to \code{YES} (for example, \code{CANONICAL})
}

\strong{VCF format:}
\itemize{
\item Standard VCF columns with CSQ in INFO field
\item CSQ contains pipe-delimited values ordered by the CSQ header definition
}

The function automatically:
\itemize{
\item Detects and handles gzipped files
\item Skips metadata + header lines
\item Replaces VEP dash placeholders ("-") with NA
\item Preserves metadata for provenance tracking
}
}
\examples{
\dontrun{
# Read all columns from tab-delimited VEP output
df <- read_vep_data("sample.filtered")

# Read only specific columns of interest
df <- read_vep_data(
  "sample.filtered",
  columns = c("Uploaded_variation", "Gene", "Consequence",
              "SYMBOL", "IMPACT", "SIFT", "PolyPhen", "LoF")
)

# Read without parsing Extra (faster, returns raw Extra string)
df <- read_vep_data("sample.filtered", parse_extra = FALSE)

# Access metadata
attr(df, "vep_header")$meta$vep_version
attr(df, "vep_header")$meta$assembly

# Read gzipped VCF format
df <- read_vep_data("sample.vep.vcf.gz", format = "vcf")
}

}
\seealso{
\code{\link{read_vep_header}} for reading just the header information
}
