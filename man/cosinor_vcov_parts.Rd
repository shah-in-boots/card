% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosinor-methods.R
\name{cosinor_vcov_parts}
\alias{cosinor_vcov_parts}
\title{Stored Covariance Parts}
\usage{
cosinor_vcov_parts(object)
}
\arguments{
\item{object}{model of class \code{cosinor}}
}
\value{
a list with \code{V} (covariance over the linear parameters), \code{sigma},
\code{nobs}, \code{df.residual} and \code{kappa}
}
\description{
Returns the parts every cosinor statistic is built from,
preferring the values stored at fit time and recomputing them from \code{xmat}
when the object predates them.
}
\details{
Both paths take \code{n} from \code{nrow(xmat)}, so a model fitted by an older
version of the package cannot disagree with a freshly fitted one.
}
\keyword{internal}
