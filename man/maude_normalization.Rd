% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maude-normalize.R
\name{maude_normalization}
\alias{maude_normalization}
\alias{normalize_maude_manufacturer}
\alias{normalize_maude_ablation}
\alias{resolve_maude_owner}
\title{Normalise MAUDE Manufacturer and Device Strings}
\usage{
normalize_maude_manufacturer(
  manufacturer_name,
  index = card::maude_manufacturer_index
)

normalize_maude_ablation(device_brand_name, index = card::maude_ablation_index)

resolve_maude_owner(entity, as_of, ownership = card::maude_ownership)
}
\arguments{
\item{manufacturer_name}{Character vector of raw MAUDE manufacturer strings,
as returned in the \code{manufacturer_name} column of \code{\link[=maude_query]{maude_query()}}.}

\item{index}{Lookup table to match against, defaulting to
\link{maude_manufacturer_index} and \link{maude_ablation_index} respectively. Supply
your own to extend or override the bundled tables; it must carry a
\code{pattern} column of regular expressions in priority order, plus the value
columns the function returns.}

\item{device_brand_name}{Character vector of raw MAUDE brand strings, as
returned in the \code{device_brand_name} column of \code{\link[=maude_query]{maude_query()}}.}

\item{entity}{Character vector of entity names, as returned by
\code{normalize_maude_manufacturer()} or in the \code{entity} column of
\code{normalize_maude_ablation()}.}

\item{as_of}{Date vector giving the point in time to resolve ownership at,
recycled against \code{entity}. There is deliberately no default: the answer
changes with it, and a \code{Sys.Date()} default would make the same code return
different results after a future acquisition without anything in the call
having changed. Pass \code{Sys.Date()} for the company that owns the entity now,
or a report's \code{date_received} for the company that owned it when the report
was filed.}

\item{ownership}{Ownership table to walk, defaulting to \link{maude_ownership}.
Must carry \code{entity}, \code{parent}, \code{from} and \code{to} columns, the last two being
\code{Date} and allowed to be missing for an unbounded interval.}
}
\value{
\code{normalize_maude_manufacturer()} returns a character vector the same
length as \code{manufacturer_name}, holding the entity name or \code{NA} where the
index does not cover the string.

\code{resolve_maude_owner()} returns a character vector the same length as
\code{entity}, holding the company that owned it on \code{as_of}. An entity that
nobody owned on that date resolves to itself, which is how a divestiture is
expressed: Physio-Control resolves to Medtronic in 2005, to itself in 2014,
and to Stryker in 2020.

\code{normalize_maude_ablation()} returns a \code{tbl_df} with one row per input and
the columns:
\describe{
\item{device_brand_name}{The input string, unchanged}
\item{platform}{The device platform, such as \code{"Arctic Front"}}
\item{modality}{One of \code{"cryoablation"}, \code{"radiofrequency"},
\code{"pulsed field"}, \code{"pulsed field or radiofrequency"}, or \code{"laser"}.
\code{NA} both for an unmatched brand and for a matched catheter that
delivers no energy, such as a mapping catheter -- check \code{platform} to
tell the two apart}
\item{entity}{The entity that makes the platform. Pass it to
\code{resolve_maude_owner()} for the corporate parent}
}
}
\description{
\code{normalize_maude_manufacturer()} maps the \code{manufacturer_name} returned by
\code{\link[=maude_query]{maude_query()}} onto the entity that made the device, collapsing plants,
spelling variants, and contract manufacturers onto one name.

\code{normalize_maude_ablation()} maps a cardiac ablation \code{device_brand_name} onto
its platform, the energy modality it delivers, and the entity that makes it.

\code{resolve_maude_owner()} follows an entity up its chain of acquisitions to the
company that owned it on a given date.
}
\details{
All three are lookups over bundled tables, and all three return \code{NA} rather
than a guess for a value the tables do not cover. See
\link{maude_manufacturer_index}, \link{maude_ablation_index} and \link{maude_ownership} for
measured coverage and for how to extend them.
}
\section{Why normalising and resolving are separate}{
Which company a string names does not change with time. Who owns that company
does. Keeping them apart is what lets one acquisition be one row rather than
a rewrite of every pattern beneath it, and it is why \code{resolve_maude_owner()}
requires \code{as_of} while the two \verb{normalize_*()} functions take no date at all:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{events |>
  dplyr::mutate(
    entity  = normalize_maude_manufacturer(manufacturer_name),
    company = resolve_maude_owner(entity, as_of = Sys.Date()),
    at_time = resolve_maude_owner(entity, as_of = date_received)
  )
}\if{html}{\out{</div>}}

\code{company} counts every St. Jude report as Abbott, which is what a question
about today's market wants. \code{at_time} counts a 2010 report as St. Jude, which
is what a question about who was shipping the device then wants. Neither is a
default, because picking one silently would answer a question the caller did
not ask.
}

\section{Why the manufacturer needs normalising}{
\code{manufacturer_name} is the manufacturing entity, not the company. Counting
the raw strings splits a single maker across its plants -- Biosense Webster
reports arrive under at least four -- and hides a company behind its contract
manufacturer, as Abbott is behind \code{"VENUSA DE MEXICO S.A. DE C.V."}.
}

\section{Why the modality needs the brand}{
Product code does not determine ablation modality. \code{OAE} covers cryoablation
and radiofrequency alike, so a rule that resolves it from the product code
and falls back to radiofrequency will put cryoballoon, pulsed-field, and
laser devices into a radiofrequency arm. Deriving the modality from the brand
is what separates them.

Where a study already assigns a modality from the product code, the useful
move is to compare the two rather than to silently prefer one:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{events |>
  dplyr::mutate(
    from_brand = normalize_maude_ablation(device_brand_name)$modality
  ) |>
  dplyr::count(from_product_code, from_brand)
}\if{html}{\out{</div>}}

Every off-diagonal cell is a report whose modality depends on which rule was
used, and the \code{NA} column is the reports neither rule can place -- including
the mapping and access catheters that share the ablation product codes and
deliver no energy at all.
}

\examples{
# Plants, spelling variants, and a contract manufacturer collapse onto one
# entity, without anyone having to say when
normalize_maude_manufacturer(c(
  "BOSTON SCIENTIFIC CORPORATION",
  "FARAPULSE, INC.",
  "BIOSENSE WEBSTER, INC. (JUAREZ)",
  "VENUSA DE MEXICO S.A. DE C.V.",
  "SOME COMPANY NOT IN THE INDEX"
))

# Ownership is the part that needs a date
resolve_maude_owner(
  "Farapulse",
  as_of = as.Date(c("2019-01-01", "2024-01-01"))
)

# A two-hop chain, resolved rather than flattened
resolve_maude_owner(
  "Telectronics Pacing Systems",
  as_of = as.Date(c("2000-01-01", "2020-01-01"))
)

# A divestiture, which a single-parent table cannot express
resolve_maude_owner(
  "Physio-Control",
  as_of = as.Date(c("2005-01-01", "2014-01-01", "2020-01-01"))
)

# Modality comes from the brand, not the product code
normalize_maude_ablation(c(
  "ARCTIC FRONT ADVANCE PRO CARDIAC CRYOABLATION CATHETER",
  "POLARX FIT",
  "FARAWAVE PULSED FIELD ABLATION CATHETER",
  "THERMOCOOL SMARTTOUCH SF",
  "PENTARAY NAV",
  "ARCTIC SUN 5000"
))

}
\seealso{
\link{maude_manufacturer_index}, \link{maude_ablation_index} and
\link{maude_ownership} for the bundled lookups, and \code{\link[=maude_query]{maude_query()}} for the
columns these read.
}
