# MAUDE Data ----
s

# MAUDE Narrative Evaluation with LLMs ----

# Need to create a function that can handle the LLM interaction for the narative text. Need the function to limit hte amount of data that is coming into the LLM to limit token use. 

# Arguments:
# - API key for the LLM service
# - What type of events it needs to focus on (this stems from standard problems listed by MAUDE, with an individual definition for each, tiered terms/hierarchy for specificity of diagnosis with some overlap)
# - Narrative text to be evaluated from MAUDE database 


get_maude_events <- function(api_key = NULL) {

}